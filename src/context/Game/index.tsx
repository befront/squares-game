import { Provider } from './Game.provider';
import { useGame, useGameState, useGameDispatch } from './Game.hooks';

export {
    Provider as GameProvider,
    useGame,
    useGameState,
    useGameDispatch
};